# Environment Configuration
GEMINI_API_KEY=
GOOGLE_MAPS_PLATFORM_KEY=

# Set to 'true' to connect the local React client to Firebase Emulator Suite (Auth port 9099, Firestore port 8080)
VITE_USE_FIREBASE_EMULATOR=false
